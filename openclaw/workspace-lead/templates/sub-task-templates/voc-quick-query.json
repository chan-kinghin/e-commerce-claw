{
  "request_id": "req_{{date}}_{{seq}}",
  "source": "lead",
  "target": "voc-analyst",
  "task_type": "quick_query",
  "priority": "{{priority|normal}}",
  "timeout_seconds": 120,
  "payload": {
    "category": "{{category}}",
    "keywords": "{{keywords}}",
    "target_market": "{{target_market|US}}",
    "platform": "{{platform|amazon}}",
    "query_type": "{{query_type|price_check}}",
    "asins": "{{asins|[]}}"
  },
  "callback_expectations": {
    "format": "QuickQueryResult JSON",
    "key_fields": ["results", "summary"],
    "downstream_consumers": []
  }
}
